source = ~/.cache/wal/colors-hyprland.conf

background {
	monitor =
path = /home/tobias/Pictures/Wallpapers/lucystreet.jpg
	blur_size = 6
	blur_passes = 1
	crossfade_time = 1
}

#Passwort Eingabe
input-field {
	monitor = 
	size = 200, 40
	outer_color = $foreground
	#outer_color = rgba(255,255,255,0.1)
	outline_thickness = 3    			# border
	inner_color = rgba(255,255,255,0)		# die box füllung unsichtbar
	font_color = $foreground
	#font_color = rgb(255,255,255)
	font_family = PFStardust
	dots_size = 0.3
	dots_spacing = 0.2
	dots_center = true
	fade_timeout = 2000
	fade_on_empty = true
	position = 0, 0           # centered
	halign = center
	valign = center
	fail_text = bruh
	placeholder_text = :)
}

#Begrüßung
label {
	monitor = 
	text = $USER
	font_family = PFStardust
	font_size = 36
	#color = rgb(255,255,255)
	color = $color1
	shadow_passes = 1
	position = 0, 350
	halign = center
	valign = center
}

#Uhr
label {
	monitor = 
	text = $TIME
	font_family = PFStardust
	font_size = 100
	color = $color2
	#color = rgb(255,255,255)
	shadow_passes = 1
	position = 0, 250
	halign = center
	valign = center
}

#Datum
label {
	monitor = 
	text = cmd[update:60000] date +"%d.%m.%Y"
	font_family = PFStardust
	font_size = 18
	color = $color3
	#color = rgb(255,255,255)
	shadow_passes = 1
	position = 0, 150
	halign = center
	valign = center
}


label {
	monitor = 
	text = cmd[update:5] echo "$(playerctl metadata title)" /  "$(playerctl metadata artist)"
	font_size = 20
	font_family = PFStardust
	color = $color1
	position = 0, 40
	halign = center
	valign = bottom

	shadow_passes = 1
	shadow_size = 1
}


general {
	hide_cursor = true
}
